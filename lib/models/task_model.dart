class Task {
  String title;
  String description;
  bool isCompleted;
  DateTime date;

  Task({
    required this.title,
    required this.description,
    required this.isCompleted,
    required this.date,
  });
}
